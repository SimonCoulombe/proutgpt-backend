# Copy this file to ~/.env and fill in your values.
# ~/.env is loaded by both start_openrouter.sh and the systemd service.
#
# NEVER commit the real ~/.env to git.

export OPENROUTER_API_KEY=sk-or-v1-REPLACE_WITH_YOUR_KEY
